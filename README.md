# NP_RuntimeStudio
NP Runtime Studio is a lightweight bridge between Visual Studio 2012 and modern AI tools.  Instead of creating temporary files, the selected code is stored as a runtime context and shared with a local Chrome Extension. Send selected code from Visual Studio 2012Display project and file information View selected code inside Chrome.Save code as a file
